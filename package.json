{
  "name": "collabdocs",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspace backend",
    "build": "npm run build --workspace frontend"
  }
}
